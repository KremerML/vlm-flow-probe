include:
  - ../fragments/model_llava15_hf.yaml
  - ../fragments/dataset_clevr_lite.yaml
dataset:
  split: "val"
knockout:
  flows: ["Image->Question", "Image->Last"]
  window: 1
  filter_correct: true
  normalize_logprob: true
experiment:
  output_base: "output/experiments"
  name: "knockout_clevr_lite"
  use_timestamp: true
